# Local development
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000

# Production (only needed when the frontend is hosted on a different origin
# than the backend, e.g. Cloudflare Pages + Render — see docs/FREE_DEPLOYMENT.md)
# VITE_API_BASE_URL=https://polysignal-api.onrender.com
# VITE_WS_URL=wss://polysignal-api.onrender.com
